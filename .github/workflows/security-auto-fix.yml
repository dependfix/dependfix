name: Security Auto Fix

on:
  workflow_dispatch:
    inputs:
      mode:
        description: "运行模式"
        type: choice
        required: true
        options:
          - report-only
          - fix
          - fix-and-pr
        default: fix-and-pr
      severity-threshold:
        description: "严重级别阈值"
        type: choice
        required: false
        options:
          - critical
          - high
          - medium
          - all
        default: all
      dry-run:
        description: "试运行模式（不修改文件，仅在 fix 模式下生效）"
        type: boolean
        required: false
        default: false
      max-alerts-per-repository:
        description: "每仓库最多处理的告警数"
        type: string
        required: false
        default: "100"
      repos:
        description: "目标仓库列表（逗号分隔 owner/repo，留空默认使用当前仓库）"
        type: string
        required: false
        default: ""
      cleanup-branches-auto:
        description: "（fix-and-pr 模式）结束后自动删除已合并/已关闭的 dependfix 分支（非交互；不删有 open PR 的分支）"
        type: boolean
        required: false
        default: true
  schedule:
    # 每周一 UTC 6:00（北京时间 14:00）
    - cron: "0 6 * * 1"

permissions:
  contents: write
  pull-requests: write
  security-events: read

concurrency:
  group: security-auto-fix-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: false

jobs:
  dependfix:
    name: Dependfix Security Scan
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Run Dependfix Action
        uses: ./
        with:
          mode: ${{ inputs.mode || 'fix-and-pr' }}
          severity-threshold: ${{ inputs.severity-threshold || 'high' }}
          dry-run: ${{ inputs.dry-run || 'false' }}
          max-alerts-per-repository: ${{ inputs.max-alerts-per-repository || '20' }}
          repos: ${{ inputs.repos || '' }}
          github-token: ${{ secrets.GITHUB_TOKEN }}
          # Dependabot alerts 读取需最小权限 PAT（fine-grained，仅 Dependabot alerts: read）；
          # GITHUB_TOKEN 无法读取该 API。仓库 Settings → Secrets 配置 GH_PAT。
          dependabot-alerts-token: ${{ secrets.GH_PAT }}
          cleanup-branches-auto: ${{ inputs.cleanup-branches-auto || 'false' }}
