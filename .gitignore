.DS_Store
node_modules

# local env files
.env.local
.env.*.local

# Log files
npm-debug.log*
yarn-debug.log*
yarn-error.log*
*.log

# Editor directories and files
.idea
.vscode
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw*

# 忽略图片
*.jpg
*.png
*.rar

.git
# ssl
# *.crt
# *.key
# *.pem
sessions
/test
*.assets
#public
dist
temp

.session

# VitePress
docs/.vitepress/dist
docs/.vitepress/cache
docs/.vitepress/.temp

# AI Agents & Skills Local Links
# 目前通过本地链接的方式来进行复用配置，故不提交到版本库中。
.claude/agents
.claude/skills
.agents/agents
.agents/skills
.opencode/agents
.opencode/skills

# OpenCode 运行时配置
# opencode.json 由 .opencode/configs/ 下的预设通过 scripts/setup/switch-models.mjs 生成，
# 不再固定单一套渠道配置，避免 PR 反复 churn 默认值。
opencode.json

# dependfix
dependfix-reports/

# test coverage
coverage/


# Review Gate 审计记录
artifacts/

# release 计划临时产物（release:plan 生成 → 人工 review → release:version 消费删除）
release-plan.md

# 发布结果临时产物（release:publish 写入 → release:github 消费）
release-publish-result.json


